{
  "mcpServers": {
    "claude-matrix": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/build/index.mjs"],
      "env": {
        "CLAUDEMATRIX_DATA_DIR": "${HOME}/.claude/local/claudematrix"
      }
    }
  }
}
