{
  "mcpServers": {
    "claudematrix": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/build/index.js"],
      "env": {
        "CLAUDEMATRIX_DATA_DIR": "${HOME}/.claude/local/claudematrix"
      }
    }
  }
}
